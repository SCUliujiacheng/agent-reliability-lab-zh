[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-reliability-lab"
version = "0.1.0"
description = "一个用于评测工具调用型智能体可靠性的本地优先平台。"
requires-python = ">=3.12"
dependencies = [
  "anyio==4.10.0",
  "fastapi==0.116.1",
  "httpx>=0.28,<1",
  "pydantic>=2.10,<3",
  "PyYAML>=6.0,<7",
  "SQLAlchemy>=2.0,<3",
  "typer>=0.16,<1",
  "uvicorn[standard]==0.35.0",
]

[project.scripts]
arl = "agent_reliability_lab.cli:app"

[dependency-groups]
dev = [
  "mypy>=1.13,<2",
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.24,<2",
  "ruff>=0.8,<1",
  "types-PyYAML>=6.0.12.20250915",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agent_reliability_lab"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 88
exclude = ["docs", "web/node_modules", "web/dist"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src"]
